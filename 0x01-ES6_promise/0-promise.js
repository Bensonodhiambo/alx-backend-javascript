export default function getResponseFromAPI() {
  return new Promise((resolve) => {
    // You can perform asynchronous operations here, but for now, we'll resolve it directly
    resolve('Success');
  });
}
